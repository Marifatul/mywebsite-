<!DOCTYPE html>
<html>
  <head>
    <% include partials/head %>
  </head>
  <body>
    <% include partials/menu %>
    <div class="container-fluid bg-3 text-center">
    <div class="row">
      <p><center>About</center></p> <br>
      <div class="col-sm-4">
        <p>This is the main page using the EJS template engine 
          This is the main page using the EJS template engine 
          This is the main page using the EJS template engine 
          This is the main page using the EJS template engine.</p>
      </div>
      <div class="col-sm-4">
        <p>This is the main page using the EJS template engine 
          This is the main page using the EJS template engine 
          This is the main page using the EJS template engine 
          This is the main page using the EJS template engine.</p>
      </div>
      <div class="col-sm-4">
        <p>This is the main page using the EJS template engine 
          This is the main page using the EJS template engine 
          This is the main page using the EJS template engine 
          This is the main page using the EJS template engine.</p>
      </div>
    </div>
  </div>
  </body>
  <% include partials/script %>
</html>
